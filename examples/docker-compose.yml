version: "3.8"

services:
  kubenexus:
    build: ..
    ports:
      - "3001:3001"
    environment:
      - PORT=3001
      - MCP_TRANSPORT=sse
      - KUBECONFIG=/app/.kube/config
    volumes:
      - ${HOME}/.kube/config:/app/.kube/config:ro
    restart: unless-stopped
