apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: agentic-ai-server-data
  namespace: openshift-mcp
  labels:
    app.kubernetes.io/name: agentic-ai-server
    app.kubernetes.io/part-of: tcs-agentic-ai
spec:
  storageClassName: vxflexos-xfs
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
