apiVersion: v1
kind: Service
metadata:
  name: {{ include "tcs-agentic-ai.fullname" . }}
  labels:
    {{- include "tcs-agentic-ai.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tcs-agentic-ai.selectorLabels" . | nindent 4 }}
